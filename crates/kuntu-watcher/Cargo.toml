# Cross-platform watcher abstraction for the independent file-search workspace.
[package]
name = "kuntu-watcher"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kuntu-core = { path = "../kuntu-core" }
kuntu-crawler = { path = "../kuntu-crawler" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
