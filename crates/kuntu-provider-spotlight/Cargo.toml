# macOS Spotlight provider crate for the file search prototype.
[package]
name = "kuntu-provider-spotlight"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kuntu-core = { path = "../kuntu-core" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
