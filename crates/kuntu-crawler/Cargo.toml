# Filesystem crawler crate for explicit-root file search indexing.
[package]
name = "kuntu-crawler"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kuntu-core = { path = "../kuntu-core" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
